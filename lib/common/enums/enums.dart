enum ControlType { leftHanded, rightHanded }

enum Direction { up, down, left, right }

enum PlayerState { left, right, none, dead }

enum MissileState { flight, explosion }

enum MissileType { friendly, enemy }

enum EnemyState { left, right, explosion }
